import Layout from "layouts/index.vue"
const routes = [
    // {path:'/',component:()=>import('views/home.vue')},
    {
        path: '/',
        name: 'Layout',
        component: Layout
    }
]

export default routes